version: '3'

tasks:
  lint:
    desc: Run golangci-lint on config module
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - golangci-lint run ./...

  lint:fix:
    desc: Run golangci-lint with auto-fix on config module
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - golangci-lint run --fix ./...

  test:
    desc: Run tests for config module
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - go test ./... -race -count=1

  tidy:
    desc: Run go mod tidy for config module
    dir: '{{.TASKFILE_DIR}}'
    cmds:
      - go mod tidy
